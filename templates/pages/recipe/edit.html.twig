{% extends 'base.html.twig' %}

{% block title %}Cookable - Modifier une Recette{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-4">Modifier une Recette</h1>
        <div class="container mx-auto col-6">
            {{ form_start(form) }}
            <div class="form-group my-1">
                {{ form_label(form.name) }}
                {{ form_widget(form.name) }}
                <div class="form-error">{{ form_errors(form.name) }}</div>
            </div>
            <div class="form-group my-1">
                {{ form_label(form.time)}}
                {{ form_widget(form.time) }}
                <div class="form-error">{{ form_errors(form.time) }}</div>
            </div>
            <div class="form-group my-1">
                {{ form_label(form.people)}}
                {{ form_widget(form.people) }}
                <div class="form-error">{{ form_errors(form.people) }}</div>
            </div>
            <div class="form-group my-1">
                {{ form_label(form.difficulty) }}
                {{ form_widget(form.difficulty) }}
                <div class="form-error">{{ form_errors(form.difficulty) }}</div>
            </div>
            <div class="form-group my-1">
                {{ form_label(form.description) }}
                {{ form_widget(form.description) }}
                <div class="form-error">{{ form_errors(form.description) }}</div>
            </div>
            <div class="form-group my-1">
                {{ form_widget(form.isFavorite) }}
                {{ form_label(form.isFavorite, null, { 'label_attr': {'class': 'form-check-label'} }) }}

                <div class="form-error">{{ form_errors(form.isFavorite) }}</div>
            </div>
            <div class="form-group my-1">
                {{ form_label(form.price)}}
                {{ form_widget(form.price) }}
                <div class="form-error">{{ form_errors(form.price) }}</div>
            </div>
            <div class="form-group my-1">
                {{ form_row(form.ingredients, ) }}
                <div class="form-error">{{ form_errors(form.ingredients) }}</div>
            </div>
            <div class="form-group my-1">
                {{ form_row(form.submit) }}
            </div>

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}